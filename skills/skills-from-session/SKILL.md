---
name: skills-from-session
description: Alias for sfs - analyze the current conversation session to identify reusable patterns and create skills for general tasks (user-level) and project-specific tasks (project-level) that don't already have corresponding skills
---

# Skills From Session (Alias)

This is an alias for the `sfs` skill.

## Usage

Use `/skills-from-session` or `/sfs` - both invoke the same skill.

## Instructions

When this skill is invoked, immediately use the Skill tool to run `sfs` with the
provided arguments.

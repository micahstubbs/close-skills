---
name: new-skills-session
description: Alias for nss - Review the current session and create new skills from reusable patterns found
---

# New Skills Session (Alias)

This is an alias for the `nss` skill.

## Usage

Use `/new-skills-session` or `/nss` - both invoke the same skill.

## Instructions

When this skill is invoked, immediately use the Skill tool to run `nss` with the
provided arguments.

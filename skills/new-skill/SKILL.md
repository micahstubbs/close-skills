---
name: new-skill
description: Alias for ns - create a new skill with a short abbreviated name and a kebab-case alias
---

# New Skill (Alias)

This is an alias for the `ns` skill.

## Usage

Use `/new-skill` or `/ns` - both invoke the same skill.

## Instructions

When this skill is invoked, immediately use the Skill tool to run `ns` with the
provided arguments.
